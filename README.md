# Web-IPAM System

**Web-based IP Address Management System** - Веб-система для управления IP-адресным пространством оператора связи.

## О проекте

Web-IPAM (IP Address Management) — это веб-приложение для учета и управления IP-адресами в сетях провайдеров и корпоративных сетях. Система позволяет централизованно вести учет IP-инфраструктуры.

## Полное руководство по возможностям и сценариям использования

**Проблемы, которые решает Web-IPAM:**

- Потеря контроля над IP-адресным пространством
- Ручное ведение Excel-таблиц с IP-адресами
- Отсутствие единой системы учета клиентов и их оборудования
- Сложности при расследовании сетевых инцидентов
- Невозможность быстро найти свободные IP-адреса
- Отсутствие аудита изменений в сети

### Ключевые возможности системы

#### 1. **Интеллектуальное управление IP-пространством**

- **Автоматический расчет подсетей** - система сама рассчитывает диапазоны, широковещательные адреса и доступные IP
- **Визуализация занятости подсетей** - сразу видно, сколько адресов свободно, занято или зарезервировано
- **Предотвращение конфликтов** - система не позволит назначить один IP разным устройствам
- **Массовые операции** - изменение статусов нескольких IP-адресов одновременно

#### 2. **Комплексный учет клиентов и оборудования**

- **Полный цикл "клиент-устройство-IP"** - от заключения договора до настройки оборудования
- **Учет MAC-адресов** - привязка физического оборудования к IP-адресам
- **История изменений** - полный аудит всех изменений в данных клиентов

#### 3. **Продвинутая система безопасности**

- **Защита от несанкционированного доступа** - ролевая модель с разделением прав
- **Блокировка bruteforce-атак** - автоматическая блокировка IP при множественных неудачных попытках входа
- **Полный аудит действий** - кто, что, когда и с какого IP изменил в системе

#### 4. **Гибкая система статусов и типов IP-адресов**

- **Типы адресов**: белые (white) и серые (gray) с различной логикой управления
- **Статусы адресов**: активные, свободные, зарезервированные - для точного контроля использования

## Реальные сценарии работы системы

### Сценарий 1: **Подключение нового клиента с белым IP-адресом**

**Ситуация**: Новый клиент требует выделение белого IP-адреса для организации VPN-сервера.

**Процесс в Web-IPAM:**

1. **Создание клиента** → Добавляем клиента с договором "ДГ-2024-004", ФИО, адресом
2. **Регистрация устройства** → Вносим маршрутизатор клиента (MAC: `BB:CC:DD:EE:FF:01`, модель: Mikrotik)
3. **Поиск белого IP** → Система показывает свободные белые IP в пуле `95.165.150.0/27`
4. **Назначение IP** → Выбираем `95.165.150.7` (статус: `free` → `active`, тип: `white`)
5. **Автоматический аудит** → Система фиксирует: "Администратор назначил белый IP 95.165.150.7 клиенту Иванов"

**Результат**: Клиент подключен за 5 минут, вся информация систематизирована, IP защищен от случайного переиспользования.

### Сценарий 2: **Массовое переподключение клиентов при смене подсети**

**Ситуация**: Миграция клиентов с подсети `10.10.0.0/23` на `10.20.0.0/23`.

**Процесс в Web-IPAM:**

1. **Создание новой подсети** → Добавляем `10.20.0.0/23` с описанием "Новая подсеть для миграции"
2. **Массовое резервирование IP** → Блокируем диапазон `10.20.0.10-10.20.0.50` (статус: `reserved`)
3. **Поэтапное перемещение** → Для каждого клиента:
   - Освобождаем старый IP в старой подсети (статус: `active` → `free`)
   - Назначаем новый IP в новой подсети (статус: `reserved` → `active`)
4. **Контроль процесса** → Дашборд показывает прогресс миграции в реальном времени

**Результат**: Миграция 40+ клиентов без потерь данных и конфликтов IP-адресов.

### Сценарий 3: **Расследование сетевой атаки**

**Ситуация**: Обнаружена DDoS-атака с IP-адреса `95.165.150.5`.

**Процесс в Web-IPAM:**

1. **Мгновенная идентификация** → Поиск по IP показывает:
   - Клиент: Козлов Дмитрий Игоревич
   - Устройство: ASUS RT-AC68U (MAC: `AA:BB:CC:DD:EE:03`)
   - Договор: ДГ-2024-003
2. **Просмотр истории** → Аудит показывает все действия с этим IP
3. **Блокировка** → Изменяем статус IP на `reserved` с комментарием "Заблокирован для расследования"
4. **Уведомление клиента** → Используем контактные данные из системы

**Результат**: Время реакции на инцидент сокращено с часов до минут.

### Сценарий 4: **Планирование расширения сети**

**Ситуация**: Необходимо оценить возможность подключения 50 новых клиентов.

**Процесс в Web-IPAM:**

1. **Анализ текущей загрузки** → Система показывает:
   - Подсеть `10.10.0.0/23`: 203 из 510 IP занято (40% загрузка)
   - Свободные белые IP: 5 из 30 (требуется расширение пула)
2. **Создание новой подсети** → Добавляем `10.30.0.0/23` для новых подключений
3. **Автоматическое создание IP** → Генерация 100 новых IP со статусом `free`
4. **Резервирование для специфичных нужд** → Блокируем IP для будущих серверов (статус: `reserved`)

**Результат**: Точно спланированное расширение сети без избыточного выделения ресурсов.

### Сценарий 5: **Работа с временными подключениями**

**Ситуация**: Организация временного мероприятия с Wi-Fi покрытием на 200 пользователей.

**Процесс в Web-IPAM:**

1. **Создание временной подсети** → `10.100.0.0/24` с описанием "Временное мероприятие 15.11.2024"
2. **Настройка DHCP-диапазона** → Резервируем `10.100.0.10-10.100.0.210` (статус: `reserved`)
3. **По окончании мероприятия** → Массовое освобождение IP (статус: `reserved` → `free`)
4. **Архивация подсети** → Добавление комментария "Мероприятие завершено"

**Результат**: Временные IP не конфликтуют с основной инфраструктурой, легкость управления.

## Уникальные преимущества Web-IPAM

### Для операторов связи:

- **Сокращение времени** на обработку заявок с 30 до 5 минут
- **Устранение человеческих ошибок** при назначении IP-адресов
- **Автоматическая документация** всех изменений в сети
- **Быстрая идентификация** проблемных узлов и клиентов

### Для инженеров:

- **Единый источник истины** по всей IP-инфраструктуре
- **Визуальное представление** загрузки подсетей
- **Инструменты для планирования** расширения сети
- **Интеграция с существующими процессами**

### Для руководства:

- **Полная прозрачность** использования сетевых ресурсов
- **Статистика и отчетность** по росту клиентской базы
- **Аудит безопасности** - кто и какие изменения вносил
- **Основа для биллинговых систем** и автоматизации

## Технические сценарии использования статусов и типов IP

### Комбинации статусов и их применение:

#### 🔵 **Active + White**

- **Назначение**: Веб-серверы, VPN-шлюзы, игровые серверы клиентов
- **Пример**: `95.165.150.5` - белый IP для VPN клиента Козлова Д.И.
- **Управление**: Требует особого контроля, лимитированный ресурс

#### 🔵 **Active + Gray**

- **Назначение**: Основная масса клиентских подключений
- **Пример**: `10.10.0.10` - серый IP для абонента Иванова А.П.
- **Управление**: Стандартное подключение, массовое использование

#### 🟢 **Free + White**

- **Назначение**: Свободные белые IP для новых премиум-клиентов
- **Пример**: `95.165.150.6` - готов к назначению новому клиенту
- **Управление**: Ценный ресурс, назначение требует согласования

#### 🟢 **Free + Gray**

- **Назначение**: Свободные адреса для новых подключений
- **Пример**: `10.10.0.15` - доступен для следующего клиента
- **Управление**: Массовое использование, автоматическое назначение

#### 🟡 **Reserved + White**

- **Назначение**: Зарезервированные белые IP для будущих проектов
- **Пример**: `95.165.150.8` - зарезервирован для корпоративного клиента
- **Управление**: Временное изъятие из оборота

#### 🟡 **Reserved + Gray**

- **Назначение**: Технические нужды, временные мероприятия
- **Пример**: `10.10.0.200-10.10.0.210` - зарезервировано для тестирования
- **Управление**: Групповое резервирование диапазонов

### Эффективность после внедрения:

- **Сокращено время** на поиск информации об IP-адресах
- **Отсутствие конфликтов** IP-адресов
- **Ускорено расследование** сетевых инцидентов
- **Сокращено время** на подключение новых клиентов
- **Аудируемость** всех изменений в сети

## Установка и настройка

### Предварительные требования

* **Веб-сервер**: Apache 2.4+ 
* **PHP**: 7.4 или выше с расширениями:
  * mysqli
  * session
  * json
* **База данных**: MySQL 5.7 или выше
* **Браузер**: Современный браузер

### Клонирование репозитория

```bash
git clone https://github.com/feronski-bkpk/web-ipam
cd web-ipam
```

### Шаг 1: Настройка базы данных

1. **Создайте базу данных**:
   
   ```sql
   CREATE DATABASE web_ipam CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. **Импортируйте структуру** из файла `sql/init.sql`:
   
   ```bash
   mysql -u username -p web_ipam < sql/init.sql
   ```

### Шаг 2: Настройка подключения к БД

Отредактируйте файл `includes/db_connect.php`:

```php
// includes/db_connect.php
$servername = "localhost";    // Адрес сервера БД
$username = "your_username";  // Имя пользователя MySQL
$password = "your_password";  // Пароль пользователя MySQL  
$dbname = "web_ipam";         // Имя базы данных
```

### Шаг 3: Размещение проекта

Разместите файлы проекта в директории веб-сервера:

- **WAMP**: `C:\wamp64\www\web-ipam\`
- **LAMP**: `/var/www/html/web-ipam/`
- **MAMP**: `/Applications/MAMP/htdocs/web-ipam/`

### Шаг 4: Проверка установки

1. Запустите веб-сервер
2. Откройте в браузере: `http://localhost/web-ipam/`
3. Должна открыться страница входа

## Тестовые пользователи

После установки используйте эти учетные записи:

| Роль          | Логин      | Пароль     | Описание                     |
| ------------- | ---------- | ---------- | ---------------------------- |
| Администратор | `admin`    | `admin123` | Полный доступ                |
| Инженер       | `engineer` | `admin123` | Управление IP и устройствами |
| Оператор      | `operator` | `admin123` | Просмотр информации          |

**Важно**: Смените пароли по умолчанию после первого входа!

## Архитектура базы данных

### Структура базы данных

![Database Schema](/C:\wamp64\www\web-ipam/assets/img/database-schema.png)

*Диаграмма взаимосвязей таблиц базы данных*

### Основные таблицы

- **`users`** - Пользователи системы (логин, хэш пароля, роль, блокировки)
- **`clients`** - Клиенты-абоненты (договор, ФИО, адрес, телефон)
- **`subnets`** - Подсети (адрес сети, маска, шлюз, описание)
- **`devices`** - Устройства (MAC-адрес, модель, серийный номер)
- **`ip_addresses`** - IP-адреса (адрес, тип, статус, привязки)
- **`system_audit_log`** - Журнал аудита (действия, пользователи, IP)
- **`security_blocks`** - Блокировки безопасности (IP, попытки входа)

### Ключевые связи и ограничения

- **Клиенты → Устройства** (один ко многим) - один клиент может иметь несколько устройств
- **Устройства → IP-адреса** (один к одному) - одно устройство привязано к одному IP
- **Подсети → IP-адреса** (один ко многим) - в подсети может быть много IP-адресов
- **Пользователи → Действия аудита** (один ко многим) - пользователь выполняет действия в системе

### Уникальные ограничения

- Уникальный номер договора для каждого клиента
- Уникальный MAC-адрес для каждого устройства
- Уникальная комбинация IP-адреса и подсети
- Один IP-адрес может быть привязан только к одному устройству

### Типы данных и статусы

**IP-адреса:**

- Тип: `white` (белые), `gray` (серые)
- Статус: `active` (активен), `free` (свободен), `reserved` (зарезервирован)

**Пользователи:**

- Роли: `admin` (администратор), `engineer` (инженер), `operator` (оператор)

## Детальная структура проекта

```
web-ipam/
├── index.php                    # Главная страница с дашбордом
├── login.php                    # Страница аутентификации
├── logout.php                   # Выход из системы с логированием
├── README.md                    # Документация проекта
│
├── includes/                    # Системные модули PHP
│   ├── auth.php                 # Аутентификация, авторизация, проверка ролей
│   ├── db_connect.php           # Подключение к MySQL, класс Database
│   ├── audit_system.php         # Расширенная система аудита и безопасности
│   ├── audit.php                # Аудит действий с IP-адресами
│   ├── config.php               # Конфигурационные константы системы
│   ├── csrf.php                 # CSRF защита, генерация и валидация токенов
│   ├── header.php               # Навигация, шапка сайта, поисковая форма
│   ├── pagination.php           # Класс Pagination для постраничной навигации
│   └── validation.php           # Валидация и санитизация входных данных
│
├── pages/                       # Функциональные модули системы
│   │
│   ├── ip-addresses/            # Управление IP-адресами
│   │   ├── list.php             # Список IP с фильтрацией и поиском
│   │   ├── add.php              # Добавление нового IP-адреса
│   │   ├── edit.php             # Редактирование существующего IP
│   │   ├── delete.php           # Удаление IP-адреса с подтверждением
│   │   └── bulk_operations.php  # Массовые операции с IP-адресами
│   │
│   ├── clients/                 # Управление клиентами-абонентами
│   │   ├── list.php             # Список клиентов с поиском
│   │   ├── add.php              # Добавление нового клиента
│   │   ├── edit.php             # Редактирование данных клиента
│   │   └── delete.php           # Удаление клиента (только если нет устройств)
│   │
│   ├── devices/                 # Управление сетевыми устройствами
│   │   ├── list.php             # Список устройств с фильтрацией
│   │   ├── add.php              # Добавление устройства с валидацией MAC
│   │   ├── edit.php             # Редактирование устройства
│   │   └── delete.php           # Удаление устройства (только если нет IP)
│   │
│   ├── subnets/                 # Управление подсетями
│   │   ├── list.php             # Список подсетей с пагинацией
│   │   ├── add.php              # Добавление подсети с расчетом диапазона
│   │   ├── edit.php             # Редактирование подсети (шлюз, описание)
│   │   ├── delete.php           # Удаление подсети с подтверждением
│   │   ├── view.php             # Детальный просмотр подсети с IP-адресами
│   │   └── calculate_range.php  # API для расчета диапазона подсети (AJAX)
│   │
│   ├── users/                   # Управление пользователями системы
│   │   ├── list.php             # Список пользователей с ролями
│   │   ├── add.php              # Добавление нового пользователя
│   │   ├── edit.php             # Редактирование пользователя
│   │   └── delete.php           # Удаление пользователя
│   │
│   ├── audit/                   # Система аудита и безопасности
│   │   ├── list.php             # Журнал аудита с фильтрацией
│   │   └── export.php           # Экспорт отчетов в CSV формате
│   │
│   └── search/                  # Поиск по системе
│       └── global.php           # Глобальный поиск по всем модулям
│
├── assets/                      # Статические ресурсы
│   ├── css/
│   │   └── style.css            # Основные стили Bootstrap + кастомные
│   │
│   ├── js/                      # JavaScript файлы
│   │   ├── main.js              # Основные скрипты, обновление времени
│   │   └── ip-form.js           # Динамическая валидация форм IP-адресов
│   │
│   └── img/                     # Изображения
│       └── database-schema.png  # Диаграмма структуры БД
│
└── sql/                         # База данных
    └── init.sql                 # Инициализация БД (версия 5.0) - таблицы и тестовые данные
```

## Описание ключевых файлов

### Системные файлы (корень)

- **`index.php`** - Главный дашборд системы со статистикой, быстрым доступом и последними действиями
- **`login.php`** - Страница входа с защитой от brute-force атак и блокировками
- **`logout.php`** - Выход из системы с очисткой сессии и логированием

### Модули includes/

- **`auth.php`** - Управление аутентификацией, проверка ролей, CSRF защита
- **`db_connect.php`** - Класс Database для безопасного подключения к MySQL
- **`audit_system.php`** - Основной класс аудита с логированием действий и безопасностью
- **`audit.php`** - Специализированные функции аудита для IP-адресов
- **`pagination.php`** - Класс для реализации постраничной навигации
- **`validation.php`** - Функции валидации IP, MAC-адресов, email

### Модуль IP-адресов

- **`list.php`** - Таблица с фильтрами по типу, статусу, подсети
- **`add.php`** - Форма с валидацией принадлежности IP к подсети
- **`edit.php`** - Редактирование с проверкой бизнес-логики
- **`bulk_operations.php`** - Массовое изменение статусов IP

### Модуль подсетей

- **`add.php`** - Создание подсети с автоматическим расчетом диапазона
- **`view.php`** - Детальный просмотр всех IP в подсети
- **`calculate_range.php`** - API для AJAX расчета сетевого диапазона

### Система аудита

- **`list.php`** - Журнал всех действий с фильтрацией по модулям и действиям
- **`export.php`** - Экспорт логов в CSV для анализа

## Ролевая модель доступа

### Администратор

- Полный доступ ко всем модулям
- Управление пользователями
- Просмотр и экспорт аудита
- Удаление любых записей

### Инженер

- Управление IP-адресами, клиентами, устройствами
- Настройка подсетей
- Нет доступа к пользователям и аудиту

### Оператор

- Просмотр информации
- Ограниченное редактирование
- Нет доступа к удалению

## Безопасность

- Хэширование паролей (bcrypt)
- Подготовленные SQL-запросы
- Валидация всех входных данных
- CSRF-токены для форм
- Защита сессий с таймаутом
- Блокировка при множественных неудачных попытках входа

## Технологии

**Backend:**

- PHP 7.4+ с ООП подходом
- MySQL 5.7+ с внешними ключами
- Сессии PHP с регенерацией ID

**Frontend:**

- HTML5, CSS3
- JavaScript (ES6+)
- Bootstrap 5.1.3
- Адаптивный дизайн

## Решение проблем

### Частые ошибки при установке

1. **Ошибка подключения к БД**:
   
   - Проверьте параметры в `db_connect.php`
   - Убедитесь, что БД `web_ipam` создана
   - Проверьте права пользователя БД

2. **Страница отображается некорректно**:
   
   - Проверьте, что PHP расширения включены
   - Проверьте права доступа к файлам
   - Проверьте логи веб-сервера

3. **Формы не отправляются**:
   
   - Проверьте настройки PHP (max_post_size, upload_max_filesize)
   - Убедитесь, что сессии работают

## Важные замечания

**Перед использованием в production:**

1. Замените пароли по умолчанию для тестовых пользователей
2. Настройте HTTPS для шифрования трафика
3. Регулярно создавайте бэкапы базы данных
4. Настройте безопасные заголовки на веб-сервере

## Поддержка

При проблемах с установкой:

1. Проверьте настройки БД в `includes/db_connect.php`

2. Убедитесь, что база `web_ipam` создана и импортирован `sql/init.sql`

3. Проверьте права доступа к файлам на веб-сервере

4. Проверьте логи ошибок PHP и MySQL
